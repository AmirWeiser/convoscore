# Copy to .env and fill in. Never commit the real .env - see .gitignore.
OPENAI_API_KEY=sk-...

# Optional - leave unset. scripts/k8s-secrets.sh generates a strong random
# value on first run and appends it here, so it stays fixed across
# redeploys without ever being a fixed/known value in a script.
# POSTGRES_PASSWORD=
