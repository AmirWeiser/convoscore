# ConvoScore

LLM-scored support conversations, end to end: API/S3 ingestion → SQS → worker → OpenAI
→ Postgres → human review UI. Full architecture and tradeoffs in `DECISIONS.md`.

*(Setup instructions land in Phase 10.)*
